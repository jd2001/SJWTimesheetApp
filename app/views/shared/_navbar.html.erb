<nav class="bg-gray-800">
  <div class="mx-auto">
    <div class="relative flex h-16 items-center justify-between">
      <div class="flex flex-1 items-center justify-right items-stretch">
        <div class="flex flex-shrink-0 items-center pl-4">
          <p class="font-bold text-white"> TimesheetApp</p>
        </div>
        <div class="flex space-x-4 ml-4 justify-end">
          <%= link_to "Summary", root_path, class: current_page(@page, 'summary') %>
          <%= link_to "Settings", home_path, class: current_page(@page, 'settings') %>
          <%= link_to "Transactions", transactions_path, class: current_page(@page, 'transactions') %>
        </div>
        <div class="flex grow justify-end mr-4">
          <%= link_to "Sign out", destroy_user_session_path, class: 'btn-primary' %>
        </div>
      </div>
    </div>
  </div>
</nav>